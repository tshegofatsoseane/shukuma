<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Shukuma</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav class="navbar navbar-expand-lg">
    <div class="container">
            <a class="navbar-brand" href="/">
                <img 
          src="/assets/newshukumalogo.png" 
          alt="Shukuma Logo"
          style="
            width: 45px; 
            height: auto; 
            filter: drop-shadow(0 10px 30px rgba(255, 107, 53, 0.4));
          "
        >
         SHUKUMA
      </a>
      <div class="ms-auto">
        <a class="btn btn-outline-primary btn-sm" href="/auth/signup">Sign Up</a>
      </div>
    </div>
  </nav>

  <div class="container my-5 py-5">
    <div class="row justify-content-center">
      <div class="col-md-6 col-lg-5">
        <div class="card p-5 shadow-lg">
          <div class="text-center mb-5">
            <div style="font-size: 4rem; filter: drop-shadow(0 0 30px rgba(255, 107, 53, 0.4)); margin-bottom: 1rem;">👋</div>
            <h2 class="fw-bold text-gradient mb-2">Welcome Back!</h2>
            <p class="text-secondary">Login to continue your fitness journey</p>
          </div>

          <% if (error) { %>
            <div class="alert alert-danger" style="background: rgba(245, 101, 101, 0.1); border: 1px solid rgba(245, 101, 101, 0.3); color: #c53030; border-radius: 12px;">
              <i class="bi bi-exclamation-circle"></i> <%= error %>
            </div>
          <% } %>

          <form method="POST" action="/auth/login">
            <div class="mb-4">
              <label for="email" class="form-label fw-semibold">Email Address</label>
              <input 
                type="email" 
                class="form-control form-control-lg" 
                id="email" 
                name="email" 
                required
                placeholder="you@example.com"
                style="padding: 1rem 1.25rem;">
            </div>

            <div class="mb-4">
              <label for="password" class="form-label fw-semibold">Password</label>
              <input 
                type="password" 
                class="form-control form-control-lg" 
                id="password" 
                name="password" 
                required
                placeholder="Enter your password"
                style="padding: 1rem 1.25rem;">
            </div>

            <button type="submit" class="btn btn-primary w-100 btn-lg mb-4">
              <i class="bi bi-box-arrow-in-right"></i> Login
            </button>
          </form>

          <div class="text-center">
            <p class="text-secondary mb-0">Don't have an account? 
              <a href="/auth/signup" class="text-gradient fw-bold" style="text-decoration: none;">Sign Up</a>
            </p>
          </div>
        </div>

        <div class="text-center mt-4">
          <a href="/" class="text-secondary" style="text-decoration: none;">
            <i class="bi bi-arrow-left"></i> Back to Home
          </a>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

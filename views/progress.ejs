<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Progress - Shukuma</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav class="navbar navbar-expand-lg">
    <div class="container">
            <a class="navbar-brand" href="/">
                <img 
          src="/assets/shukumalogo-removebg-preview.png" 
          alt="Shukuma Logo"
          style="
            width: 45px; 
            height: auto; 
            filter: drop-shadow(0 10px 30px rgba(255, 107, 53, 0.4));
          "
        >
         SHUKUMA
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto align-items-center">
          <li class="nav-item"><a class="nav-link" href="/dashboard"><i class="bi bi-speedometer2"></i> Dashboard</a></li>
          <li class="nav-item"><a class="nav-link" href="/workouts/shuffle"><i class="bi bi-shuffle"></i> Shuffle</a></li>
          <li class="nav-item"><a class="nav-link" href="/progress"><i class="bi bi-graph-up"></i> Progress</a></li>
          <li class="nav-item"><a class="nav-link" href="/challenges"><i class="bi bi-trophy"></i> Challenges</a></li>
          <li class="nav-item"><a class="nav-link" href="/challenges/teams"><i class="bi bi-people"></i> Teams</a></li>
          <li class="nav-item"><span class="nav-link"><i class="bi bi-person-circle"></i> <%= typeof user !== 'undefined' ? user.username : '' %></span></li>
          <li class="nav-item"><a class="nav-link" href="/auth/logout"><i class="bi bi-box-arrow-right"></i></a></li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container my-5">
    <div class="text-center mb-5">
      <h1 class="display-3 fw-bold text-gradient mb-3">Your Progress</h1>
      <p class="text-secondary fs-4">Track your fitness journey</p>
    </div>

    <!-- Summary Stats -->
    <div class="row g-4 mb-5">
      <div class="col-md-4">
        <div class="stat-card">

          <div class="stat-value"><%= streak %></div>
          <div class="stat-label">Current Streak</div>
        </div>
      </div>

      <div class="col-md-4">
        <div class="stat-card">
          <div class="stat-value"><%= totalWorkouts %></div>
          <div class="stat-label">Total Completed</div>
        </div>
      </div>

      <div class="col-md-4">
        <div class="stat-card">

          <div class="stat-value"><%= progressLog.length %></div>
          <div class="stat-label">Recent Activities</div>
        </div>
      </div>
    </div>

    <!-- Streak Milestones -->
    <div class="card p-4 mb-5">
      <h4 class="mb-4 text-gradient"><i class="bi bi-award"></i> Streak Milestones</h4>
      <div class="d-flex justify-content-center gap-3 flex-wrap">
        <div class="badge <%= streak >= 3 ? 'bg-primary' : 'bg-secondary' %>" style="padding: 1rem 1.5rem; font-size: 1rem;">
          <%= streak >= 3 ? '✓' : '🔒' %> 3 Day Streak
        </div>
        <div class="badge <%= streak >= 7 ? 'bg-warning' : 'bg-secondary' %>" style="padding: 1rem 1.5rem; font-size: 1rem;">
          <%= streak >= 7 ? '✓' : '🔒' %> 7 Day Streak
        </div>
        <div class="badge <%= streak >= 14 ? 'bg-info' : 'bg-secondary' %>" style="padding: 1rem 1.5rem; font-size: 1rem;">
          <%= streak >= 14 ? '✓' : '🔒' %> 14 Day Streak
        </div>
        <div class="badge <%= streak >= 30 ? 'bg-success' : 'bg-secondary' %>" style="padding: 1rem 1.5rem; font-size: 1rem;">
          <%= streak >= 30 ? '✓' : '🔒' %> 30 Day Streak
        </div>
      </div>
    </div>

    <!-- Progress Log -->
    <div class="card p-4">
      <h3 class="mb-4 text-gradient"><i class="bi bi-list-check"></i> Recent Workouts</h3>

      <% if (progressLog.length === 0) { %>
        <div class="text-center py-5">
          <div style="font-size: 5rem; opacity: 0.3; margin-bottom: 1.5rem;">📭</div>
          <p class="text-muted fs-5 mb-4">No workouts completed yet. Time to get started!</p>
          <a href="/workouts/shuffle" class="btn btn-primary btn-lg">
            <i class="bi bi-shuffle"></i> Start Your First Workout
          </a>
        </div>
      <% } else { %>
        <% progressLog.forEach((log, index) => { %>
          <div class="progress-item">
            <div class="d-flex align-items-center gap-3">
              <div style="font-size: 2rem; filter: drop-shadow(0 0 15px rgba(255, 107, 53, 0.3));">
                <%= 
                  log.workoutId && log.workoutId.category === 'strength' ? '💪' :
                  log.workoutId && log.workoutId.category === 'cardio' ? '❤️' :
                  log.workoutId && log.workoutId.category === 'flexibility' ? '🧘' : '⚖️'
                %>
              </div>
              <div>
                <strong class="fs-5"><%= log.workoutId ? log.workoutId.name : 'Unknown Workout' %></strong>
                <br>
                <span class="badge <%= 
                  log.workoutId && log.workoutId.category === 'strength' ? 'bg-danger' :
                  log.workoutId && log.workoutId.category === 'cardio' ? 'bg-warning' :
                  log.workoutId && log.workoutId.category === 'flexibility' ? 'bg-success' : 'bg-info'
                %>" style="font-size: 0.75rem;">
                  <%= log.workoutId ? log.workoutId.category.toUpperCase() : 'N/A' %>
                </span>
              </div>
            </div>
            <div class="text-end">
              <small class="text-muted">
                <%= new Date(log.completedAt).toLocaleDateString('en-US', { 
                  month: 'short', 
                  day: 'numeric',
                  hour: '2-digit',
                  minute: '2-digit'
                }) %>
              </small>
            </div>
          </div>
        <% }) %>
      <% } %>
    </div>

    <!-- CTA -->
    <% if (progressLog.length > 0) { %>
      <div class="text-center mt-5">
        <a href="/workouts/shuffle" class="btn btn-primary btn-lg px-5">
          <i class="bi bi-shuffle"></i> Continue Training
        </a>
      </div>
    <% } %>
  </div>

  <footer class="text-center py-5 mt-5">
    <div class="container">
      <p class="text-secondary mb-2"><i class="bi bi-lightning-charge-fill text-gradient"></i> &copy; 2024 Shukuma</p>
      <p class="text-muted small">Made with ❤️ by Yanga Ngcayisa</p>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/main.js"></script>
</body>
</html>